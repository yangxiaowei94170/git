server:
  port: 8080
spring:
  datasource:
    platform: mysql
    type: com.alibaba.druid.pool.DruidDataSource
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/Shiro?useUnicode=true&useSSL=false&characterEncoding=utf8&serverTimezone=UTC
    username: root
    password: lanxin
  redis:
    host: localhost
    port: 6379
mybatis-plus:
  mapper-locations: classpath:mappers/*.xml #指定映射文件
  configuration:
    # 这个配置会将执行的sql打印出来，在开发或测试的时候可以用
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

